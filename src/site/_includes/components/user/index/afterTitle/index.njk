---
layout: layout.njk
description: This is the personal blog of Rafał Hirsz. Read about all the things I've done and all the thoughts I had.
---

<div class="listing">
{%- for year, posts in collections.postsByYear -%}
  <h1 class="listing__year">{{year}}</h1>
  <div class="listing__posts">
    {%- for post in posts -%}
      <article class="post listing__post">
        <a href="{{post.url}}" class="post__date">
          <time datetime="{{post.date | isoDate}}">{{post.date | readableDate}}</time>
        </a>
        <h1 class="post__title">
          <a href="{{post.url}}">{{post.title}}</a>
        </h1>
        <p class="post__summary">{{post.summary}}</p>
      </article>      
    {%- endfor -%}
  </div>
{%- endfor -%}
</div>
